import { SCREEN, THEME } from '@/lib/constants/common';

export type LinearGradientColors = readonly [string, string, ...string[]];

export type Theme = `${THEME}`;

export type Screen = `${SCREEN}`;
